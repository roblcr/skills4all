{% extends 'base.html.twig' %}

{% block title %}Édition de {{ car.name }}{% endblock %}

{% block body %}
    <div class="container">
        <h1 class="mb-4">Édition de {{ car.name }}</h1>

        <div class="card">
            <div class="card-body">
                {{ include('car/_form.html.twig', {'button_label': 'Editer'}) }}
            </div>
        </div>

        <a href="{{ path('app_car_index') }}" class="btn btn-primary mt-3">Retour à la liste</a>

        {{ include('car/_delete_form.html.twig') }}
    </div>
{% endblock %}
